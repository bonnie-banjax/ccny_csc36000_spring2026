[pytest]
markers =
    schema: schema/protobuf quality checks
    correctness: functional correctness checks
    grpc_impl: gRPC coordinator/worker implementation checks
    resilience: error handling and invalid input checks
    testing_repro: testing and reproducibility checks
    documentation: documentation checks
