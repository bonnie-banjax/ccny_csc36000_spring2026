# run this/these from the folder that has "core" and "tests"
the system_test.log is just a text file, and it will get over-written
every times you run the test-- leaving as an example for what output should be
 ---
 
# POSIX version
## python3 -m tests.quick-test-for-breakage

 ---
 
# Windows version
## idk create an alias for python called "python3" mb y'all

 ---
 